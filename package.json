{
  "name": "the-tweetfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "mkdir -p api && npx esbuild server/v1.ts --bundle --platform=node --outfile=api/v1.js && npx esbuild server/page.ts --bundle --platform=node --outfile=api/page.js && npx esbuild server/not-found.ts --bundle --platform=node --outfile=api/not-found.js && tsc --noEmit && vite build && vite build --ssr scripts/prerender.ts --outDir .ssr && node .ssr/prerender.js",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "test": "bun run build && bun test",
    "verify": "bun run scripts/verify.ts",
    "format": "prettier --write ."
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-icons": "^5.6.0",
    "react-router-dom": "^7.17.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "@types/bun": "1.3.9",
    "@types/node": "^25.9.1",
    "@types/react": "^19.2.16",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.2",
    "prettier": "^3.8.3",
    "tailwindcss": "^4.3.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}